class OnsPreviewModel {
  String title;
  String subTitle;
  int price;
  bool isSelected;
  OnsPreviewModel({
    required this.title,
    required this.subTitle,
    required this.price,
    required this.isSelected,
  });
}
